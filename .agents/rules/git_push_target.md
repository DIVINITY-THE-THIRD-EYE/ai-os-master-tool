# Git Push Target Rule

## Guideline
When performing git push operations or packaging releases for this project:
- Target **ONLY** the dedicated skill repository (`ai-os-v4-skill`) or `ai-os-v4/ai-os-multi-agent-skill/`.
- Do **NOT** push changes to other remotes or alter the target repository scope unless the user explicitly requests a change.
